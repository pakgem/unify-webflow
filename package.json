{
  "name": "website-animations",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "build:webflow": "node scripts/build-webflow-export.mjs",
    "build:app": "vite build --mode app",
    "preview": "vite preview --host 127.0.0.1",
    "typecheck": "tsc --noEmit",
    "audit:layout": "node scripts/audit-layout.mjs",
    "test:pressure": "node scripts/pressure-test.mjs"
  },
  "dependencies": {
    "@vercel/blob": "^2.4.0",
    "gsap": "^3.13.0"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "typescript": "^5.9.3",
    "vite": "^7.1.12"
  }
}
